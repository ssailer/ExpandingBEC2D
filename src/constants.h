#ifndef EXP2D_CONSTANTS_H__
#define EXP2D_CONSTANTS_H__

#include <iostream>
#include <complex>

 const complex<double> zero = complex<double>(0,0);
 const complex<double> half = complex<double>(0.5,0);
 const complex<double> one = complex<double>(1.0,0);
 const complex<double> two = complex<double>(2.0,0);
 const complex<double> four = complex<double>(4.0,0);
 const complex<double> six = complex<double>(6.0,0);
 const complex<double> eight = complex<double>(8.0,0);
 const complex<double> twelve = complex<double>(12.0,0); 
 const complex<double> sixteen = complex<double>(16.0,0);
 const complex<double> thirty = complex<double>(30.0,0.0);

 const complex<double> i_unit = complex<double>(0.0,1.0);

#endif // EXP2D_CONSTANTS_H__